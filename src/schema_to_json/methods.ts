type DbConfig = {};
type DbSchema = {};

// TODO: type dbconfig
export function getTableFromDB(dbConfig: DbConfig): string[][] {
  console.log(dbConfig);
  return [];
}
export function tableToSchema(table: string[][]): DbSchema {
  console.log(table);
  return {};
}
